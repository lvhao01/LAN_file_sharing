# 局域网文件共享系统

一个基于 Nuxt 3 的现代化局域网文件共享系统，支持文件上传、下载、搜索、管理等功能。

## ✨ 主要特性

### 📁 文件管理
- **文件浏览**：支持多级目录浏览，展示文件名、保存日期、文件大小
- **文件上传**：支持多文件上传，分片上传，断点续传
- **文件下载**：流式下载，支持大文件和断点续传
- **文件删除**：支持文件和文件夹删除
- **文件夹创建**：快速创建新文件夹

### 🔍 智能搜索
- **中文搜索**：支持中文文件名搜索
- **拼音搜索**：支持拼音全拼、首字母搜索（如输入 "wj" 可搜索到 "文件"）
- **英文搜索**：支持英文文件名搜索
- **实时搜索**：输入即时过滤结果

### 🎯 便捷功能
- **二维码下载**：为每个文件生成下载二维码，手机扫码即可下载
- **拖拽移动**：支持文件拖拽到文件夹或面包屑路径
- **面包屑导航**：快速跳转到上级目录
- **响应式设计**：适配桌面和移动设备

### 🎨 界面设计
- **现代化 UI**：渐变背景、毛玻璃效果、流畅动画
- **视觉反馈**：拖拽高亮、悬停效果、加载状态
- **友好交互**：清晰的操作提示和错误提示

## 🚀 快速开始

### 环境要求
- Node.js 22 及以上版本
- npm 或 yarn 包管理器

### 安装步骤

1. **安装依赖**
   ```bash
   npm install
   ```

2. **启动开发服务器**
   ```bash
   npm run dev
   ```
   服务将在 `http://localhost:5000` 启动

3. **生产环境构建**
   ```bash
   npm run build
   npm start
   ```

### 访问地址

启动后，控制台会输出局域网访问地址，例如：
- 本机访问：`http://localhost:5000`
- 局域网访问：`http://192.168.1.7:5000`（具体 IP 以控制台输出为准）

## 📖 使用说明

### 文件操作

#### 上传文件
1. 点击右上角"上传文件"按钮
2. 选择文件或拖拽文件到上传区域
3. 支持多文件同时上传
4. 上传过程中显示进度条和文件信息

#### 下载文件
1. 点击文件行的"下载"按钮
2. 浏览器会自动开始下载（支持断点续传）
3. 或点击"二维码"按钮，用手机扫码下载

#### 创建文件夹
1. 点击右上角"新建文件夹"按钮
2. 输入文件夹名称
3. 点击"创建"完成

#### 删除文件/文件夹
1. 点击文件或文件夹行的"删除"按钮
2. 确认删除操作

### 搜索功能

在搜索框输入关键词，支持：
- **中文搜索**：直接输入中文文件名
- **拼音搜索**：
  - 全拼：输入 `wenjian` 可搜索到"文件"
  - 首字母：输入 `wj` 可搜索到"文件"
- **英文搜索**：输入英文文件名

### 文件移动

1. **拖拽到文件夹**：将文件拖拽到目标文件夹行
2. **拖拽到面包屑**：将文件拖拽到面包屑路径段
3. **移动端**：长按文件 500ms 后开始拖拽

## 🛠️ 技术栈

- **前端框架**：Nuxt 3 (Vue 3)
- **样式**：SCSS
- **二维码**：qrcode
- **拼音搜索**：pinyin-pro
- **服务端**：Nitro (H3)

## 📁 项目结构

```
├── app.vue              # 主应用组件
├── assets/
│   └── main.scss        # 样式文件
├── server/
│   └── api/             # API 路由
│       ├── list.get.ts          # 文件列表
│       ├── download/[...path].get.ts  # 文件下载
│       ├── upload/               # 文件上传
│       ├── delete.post.ts        # 删除文件
│       ├── create-dir.post.ts    # 创建文件夹
│       └── move.post.ts          # 移动文件
├── SpecializedFolder/   # 共享文件夹（自动创建）
└── package.json         # 项目配置
```

## ⚙️ 配置说明

### 端口配置

默认端口为 `5000`，可在 `nuxt.config.ts` 或启动命令中修改：

```bash
npm run dev -- --port 3000
```

### 共享文件夹

默认共享文件夹为项目根目录下的 `SpecializedFolder`，首次运行会自动创建。

## 🔒 安全说明

- 当前版本仅适用于局域网环境
- 建议在生产环境添加身份验证
- 文件路径已做安全校验，防止路径遍历攻击

## 📝 更新日志

### v1.0.0
- ✅ 基础文件浏览和下载功能
- ✅ 文件上传（分片上传，断点续传）
- ✅ 智能搜索（中文、拼音、英文）
- ✅ 二维码下载
- ✅ 文件拖拽移动
- ✅ 文件夹创建和管理
- ✅ 现代化 UI 设计

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📄 许可证

MIT License